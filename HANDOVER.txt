HBlink4 HANDOVER
================
Updated: 2026-08-30

Repository
----------
Canonical repository: https://github.com/alhedward/HBlink4.git
Default branch: main
Current main: c84df73b74835cbfcb6c13b06f7cf919f2d006e0
Current fix branch: fix/parrot-ambe-49bit-packing
Dashboard semantic version: 1.3.3

Production / diagnostic state
-----------------------------
- Deployment repository: https://github.com/alhedward/hblink-website.git.
- hblink-website main currently pins HBlink4 c84df73b74835cbfcb6c13b06f7cf919f2d006e0, the temporary deterministic TG9990 voice diagnostic.
- The operator supplied hblink9990-voice.pcap after that rollout; the capture contains the deterministic diagnostic report, confirming the intended diagnostic path was active on RF.
- TG9990 parrot echo remains local-only and works correctly.
- Dashboard /admin retains the protected Parrot voice telemetry On/Off control and Apply & restart action.
- Live parrot.voice_telemetry_enabled remains operator-owned and must be preserved by deployment.
- Production runs HBlink4 natively under systemd, not Docker.
- Live /opt/hblink4/app/config/config.json is operator-owned; preserve it across upgrades.

New RF capture findings: 49-bit root cause confirmed
----------------------------------------------------
- hblink9990-voice.pcap contains a clean inbound TG9990 call, its byte-identical parrot echo, and the full deterministic telemetry stream.
- Capture split: 86 inbound DMRD packets, 86 byte-identical echo packets, then 237 telemetry packets.
- Telemetry contains three voice headers, 233 voice bursts (699 AMBE frames), and one terminator.
- The leading/trailing telemetry silence frames match the known DMR silence pattern.
- DMR framing/interleave remains proven good; the defect is inside the 49 AMBE voice parameter bits.
- Pinned OpenDMR opendmr.cpp serializes b[0..8] sequentially with widths 7,5,5,9,7,5,4,4,3.
- OpenDMR's own OP25-derived MBEEncoder encode_49bit() uses a different mapping and scatters low-order bits through positions 35..48.
- Captured telemetry interpreted with the sequential wrapper layout recovers sane encoder parameters; interpreted as encode_49bit() data, roughly half the frames produce impossible b[1] values greater than 16.
- Recovering b[0..8] from the sequential layout and repacking with encode_49bit() restores valid encoder parameter semantics without re-vocoding.
- Production-derived regression vector:
    legacy canonical:   b40930d10843ce4769
    recovered b[]:      [90, 0, 6, 87, 78, 8, 14, 13, 1]
    corrected canonical:b0357c6213d29f05c5
    corrected DMR AMBE: d5ee220741b9680b2f

Permanent fix branch
--------------------
- fix/parrot-ambe-49bit-packing starts from diagnostic main c84df73b74835cbfcb6c13b06f7cf919f2d006e0.
- hblink4/parrot_ambe.py now contains a lossless legacy sequential-b[] -> encoder encode_49bit() repacker and canonical FEC rebuild.
- hblink4/parrot_voice_assets.py applies that migration in memory while loading the pinned legacy vocabulary. The committed archive remains the original reproducible build artifact.
- No TTS, ffmpeg, OpenDMR executable or runtime vocoder is introduced.
- The established DMR interleave, HomeBrew packet builder, colour code, slot handling, timing, routing and admin On/Off control remain unchanged.
- Exact capture-derived regression coverage is added to tests/test_parrot_voice.py.
- Runtime asset metadata records the legacy archive layout and repaired encode_49bit() representation.
- The temporary hblink4/parrot_diag.py module, its run.py hook and its focused diagnostic test are removed, restoring normal live RF telemetry values.

Safety / invariants
-------------------
- TG9990 stays local-only and absent from ordinary routed talkgroup lists.
- Do not run duplicate HBlink4 cores.
- Preserve live operator routing/config and the admin-selected voice telemetry On/Off value.
- Keep Cognito primary authentication plus the local PBKDF2 break-glass administrator.
- Never expose HomeBrew/OpenBridge passphrases, full config backups, password hashes, Cognito credentials or tokens.
- Production must never run gTTS/ffmpeg/OpenDMR/vocoder tooling.

Validation and deployment next steps
------------------------------------
1. Run the complete Python regression through the normal HBlink4 PR workflow using real dependencies.
2. Merge the fix only if CI is green.
3. Pin the exact merged HBlink4 SHA in hblink-website on a fresh deployment branch.
4. Run normal hblink-website Terraform/checksum/policy CI; do not bypass gates.
5. Merge only if green and let normal main production CD deploy while preserving live config.
6. Verify installed HBlink4 SHA and healthy core/dashboard/Caddy.
7. Operator enables voice telemetry and performs the RF listening acceptance test on a real radio.
8. If speech is intelligible, retain the lossless repack solution. Whole-phrase PCM re-vocoding is unnecessary unless RF listening reveals a separate vocoder-quality issue.
